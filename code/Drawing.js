class Drawing {
  constructor(shape, size, color, position, rotation) {
    this.shape = shape;
    this.size = size;
    this.color = color;
    this.position = position;
    this.rotation = rotation;
  }
}
